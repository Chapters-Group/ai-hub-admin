services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chapters
      POSTGRES_PASSWORD: chapters
      POSTGRES_DB: chapters_admin
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
